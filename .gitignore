# Exclude query result outputs — may contain sensitive server/database info
Results/
**/Results/

# Exclude zip archives
*.zip

# Local-only reference file
Azure SQL Database Diagnostic Information Queries.sql
